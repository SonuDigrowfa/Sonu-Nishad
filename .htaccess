RewriteEngine On

# Remove .html extension from URL
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^([^/]+)$ $1.html [L]

# Optional: redirect users who type .html to the clean URL
RewriteCond %{THE_REQUEST} \s/([^\s]+)\.html [NC]
RewriteRule ^ %1 [R=301,L]
