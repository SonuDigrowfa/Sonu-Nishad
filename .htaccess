RewriteEngine On

# Redirect requests without .html to actual .html files
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^([^/]+)$ $1.html [L]

# Optional: Redirect users from .html URLs to clean ones (SEO friendly)
RewriteCond %{THE_REQUEST} \s/([^\s]+)\.html [NC]
RewriteRule ^ %1 [R=301,L]
